const Joi = require('@hapi/joi');
// const moment = require('moment');
// const params = require('../../params');
// const {makeKey} = require('../../utilities');

exports.post = Joi.object().keys({
    
});

exports.editPost = Joi.object().keys({
    
});